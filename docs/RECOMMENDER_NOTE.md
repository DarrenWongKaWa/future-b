# Note for a recommender

The student closed a first-principles Monte Carlo acceleration attempt
without inflating the result. They found real bugs in a production
Fortran diagrammatic Monte Carlo code, implemented exact-corrected
delayed acceptance, and produced LiF-electron formation energies at a
frozen discrete Hamiltonian (\(Q\approx-0.25\,\mathrm{eV}\), not the
unshifted \(\sim 9\,\mathrm{eV}\) ratio).

They froze a statistical protocol before the final timing batch, then
refused to call a \(\sim 4\%\) wall-time point estimate a success when
the predeclared bootstrap interval still crossed 5% and HAC diagnostics
failed on some chains. They also refused to relabel a tied R1/CSE
evaluator benchmark, an untrained learning recipe, or an incomplete
grouped-sampling derivation as a material acceleration.

The letter-worthy part is the combination of low-level many-body
implementation, real-material execution, and statistical honesty—not a
claim that FEP-DMC was generally accelerated by AI.
