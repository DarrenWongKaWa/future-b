# `future_b` Python package

Analysis layer for the closed project: P1 algebra, formation-energy
shift, HAC/bootstrap classification, and frozen R1 status.

Fortran excerpts under `fortran/` are Perturbo-derived (GPL-3) and are
for source-structure audit, not a complete native build.
