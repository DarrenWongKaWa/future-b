# Small public fixture

Synthetic occupancy-reverse example used by unit tests. Not a native
LiF snapshot.

`swap_row.json` is one legal cheap-state swap for P1 log-score
antisymmetry. It does not contain \(g\) or environment matrices.
