# Public C0 adapter

Source-only refresh of `vn1%wq` after `sample_q_omp_int` in
`add_external_ph`. Supported tree: public FEP-DMC commit
`05d08449cffdbd0dfbbbf5009add5cc887bc754b`.

This is **not** P1, **not** a compiled plugin, and **not** a LiF runtime
result. Historical `patches/c0_wq/` remains a private-tree lock and is
not this adapter.

See [`docs/C0_PUBLIC_ADAPTER.md`](../../docs/C0_PUBLIC_ADAPTER.md).
