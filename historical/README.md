# Historical tooling (not runtime)

Files here are kept as evidence. They are **not** part of the
installable `future_b` package or the PyPI wheel.

`c2_oracle.py` reconstructs a cheap P1 log-score from dumped swap
rows plus a private LiF `enk.dat` table. It is not a public API.
