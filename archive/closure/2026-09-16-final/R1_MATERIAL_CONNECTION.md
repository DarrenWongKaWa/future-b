# R1 material connection

**Disposition: `R1_MATERIAL_CONNECTION_NOT_CLOSED`**

The already-defined candidate is M5 Route B: outer variable-order sampling plus a bounded four-vertex exact local group with complete boundary. It is not a new global grouped algorithm.

That object is legal on the **scalar positive toy** (M4/M5). Native Luo remains `BOUND_C` with target `abs(real(D(C)))`. `SHARED_X_GROUP` / `F_n` is a different object; native grouped MH is `NOT_READY_SAMPLER_DERIVATION`.

`abs(sum D) != sum abs(D)` is a finite-domain fact (`x19.csv` crossing-minus). Native `abs(real(D))` is a third measure. A short derivation cannot transport toy heat-bath identities onto LiF EZ without a new independent check of target, proposal, reverse, window probability, open legs, and Q numerator/denominator.

Preserved scoped negative: fixed-order R1 vs CSE ≈ 0.9992 (evaluator, same object). That is **not** “all grouping is impossible.”

No sampler was implemented in this closure.
