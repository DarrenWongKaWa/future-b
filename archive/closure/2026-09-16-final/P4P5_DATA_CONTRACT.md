# P4/P5 data contract

**Disposition: `P4P5_DATA_CONTRACT_NOT_CLOSED`**

Existing dumps do not contain the frozen 64-d `p(X)`, `p(Y)` packets, unclipped `ell_prop`, whole-chain TRAIN/DEV/HOLDOUT joined to finite `ellR`, reservoir inclusion probabilities, or skippable exact wall times.

Finite `ellR` exists on C2_tonight shadow / C2_fixture ON (one short chain each). That is not the C3 packet contract. No new features were added in closure.

Historical Group-v3 `NO_DEMONSTRATED_GAIN_FIXED_RECIPE` is preserved separately and is not this P4/P5 recipe.
